export { BaseLayer } from './baseLayer';
export { layerRegistry, useLayerRegistry } from './registry';

export type {
    IMapLayer,
    IDataLayer,
    IFilterableLayer,
    IInteractiveLayer,
    LayerConfig,
} from './types';